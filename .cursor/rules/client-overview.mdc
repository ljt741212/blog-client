---
description: Client 前端项目总览规则（Monorepo 结构说明）
globs: client/**/*
alwaysApply: false
---

# Client 前端项目总览

## 项目结构

Client 是一个 Monorepo 项目，使用 pnpm workspace 管理多个应用和共享包：

```
client/
├── apps/                    # 应用目录
│   ├── blog/               # 博客前端应用（Next.js）
│   └── admin/              # 管理后台应用（React Router）
├── packages/                # 共享包目录
│   ├── behaviorMonitor/    # 行为监控 SDK
│   └── markdownEditor/     # Markdown 编辑器组件
└── package.json            # 根 package.json
```

## 应用说明

### Blog 应用 (`apps/blog`)
- **框架**: Next.js 16 App Router
- **用途**: 用户端博客展示
- **详细规范**: 参考 `client-apps-blog.mdc`

### Admin 应用 (`apps/admin`)
- **框架**: React Router 7
- **用途**: 管理后台
- **详细规范**: 参考 `client-apps-admin.mdc`

## 通用规范

两个应用都遵循以下通用规范（继承自项目根目录的 `project-standards.mdc`）：

- **语言**: 注释、变量命名、提交信息使用中文或英文均可；生成代码时的注释优先中文
- **格式**: 单引号、分号、2 空格缩进、printWidth 100、尾逗号 es5、箭头函数单参数省略括号
- **导入顺序**: react → 第三方库 → 内部别名 → 父级/兄弟/index → type 导入
- **路径别名**: 使用别名引用模块，禁止跨级相对路径引用
- **未使用变量**: 以 `_` 开头的参数或变量视为有意忽略

## 选择正确的规则

根据当前编辑的文件路径，选择对应的规则：

- **编辑 `apps/blog/` 下的文件**: 遵循 `client-apps-blog.mdc` 规范
- **编辑 `apps/admin/` 下的文件**: 遵循 `client-apps-admin.mdc` 规范
- **编辑根目录或共享包**: 遵循通用规范

## 共享包

### behaviorMonitor
- **用途**: 用户行为监控 SDK
- **构建**: Rollup
- **输出**: CJS、ESM、UMD

### markdownEditor
- **用途**: Markdown 编辑器组件
- **核心**: ByteMD
- **使用**: 在 Admin 应用中用于文章编辑

## 开发命令

```bash
# 开发
pnpm dev:blog          # 启动博客应用
pnpm dev:admin         # 启动管理后台

# 构建
pnpm build:blog        # 构建博客应用
pnpm build:admin       # 构建管理后台
pnpm build             # 构建所有应用

# 代码质量
pnpm lint              # 代码检查
pnpm format            # 代码格式化
```

## 注意事项

- 两个应用使用不同的框架和路由系统，注意区分
- Blog 使用 Next.js App Router，优先服务端组件
- Admin 使用 React Router，支持 SSR（当前配置为 false）
- 两个应用共享一些依赖（如 Ant Design、Tailwind CSS），但使用方式可能不同
- 路径别名配置不同：Blog 使用 `@/*` 指向根目录，Admin 使用 `@/*` 和 `~/` 指向 `app/` 目录
